##Each todo is structured as f"{moduleName}; {functionName}; {todo}"

Binance; getKlines; move the stats away to DataScience

General: Figure out the use of breakpoints both in general and with aid of Jupyter at last!

DataScience; plotly_closes; 1) rename the function 2) make it work with arbitrary n, auto-deciding on how many things to plot in color 3) add an argument for what to use as labels